width = float(input("Enter the width of the rectangle in cm : "))
length = float(input("Enter the length of the rectangle in cm : "))

area = width * length

print("The area of the rectacngle is : ", round(area, 2), "cm squared")